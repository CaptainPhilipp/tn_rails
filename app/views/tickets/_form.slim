= form_for(ticket, class: 'form-horizontal') do |f|
  = render 'common/error', resource: ticket
  
  = render 'common/form/form-group', f: f, key: :train do
    div = @train.number

  = render 'common/form/form-group', f: f, key: :departure do
    div = @departure.title
      
  = render 'common/form/form-group', f: f, key: :arrival do
    div = @arrival.title

  = render 'common/form/text_field', f: f, key: :passeneger_name
    
  = render 'common/form/text_field', f: f, key: :passeneger_document
    
  = hidden_field_tag :departure_id, @departure.id, name: 'ticket[departure_id]'
  = hidden_field_tag :arrival_id, @arrival.id, name: 'ticket[arrival_id]'
  = hidden_field_tag :train_id, @train.id, name: 'ticket[train_id]'
  
  = render 'common/form/submit', f: f, object: ticket, translate_path: 'common.buy'
